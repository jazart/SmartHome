query UserQuery($name: String!) {
    user(name: $name) {
        username
        firstName
        lastName
        devices {
            name
            status
            commands
        }
    }
}